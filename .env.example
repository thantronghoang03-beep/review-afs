ANTHROPIC_API_KEY=your-key-here
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Optional — set to "true" to test the full upload → xử lý → kết quả flow without
# calling Claude (no ANTHROPIC_API_KEY needed, no API cost). Returns a fixed sample
# review instead of a real one. Leave unset/false for production.
MOCK_AI_REVIEW=false
